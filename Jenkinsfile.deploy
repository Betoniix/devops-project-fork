pipeline {
    agent any
    
    environment {
        VERSION = sh(script: 'echo params.build')
    }
    
    stages {
        stage('Deploying') {
            steps {
                sh 'docker run -d --network=elastic-network -p 3301:3301 --name segunda-entrega-\$BRANCH_NAME_CLEANED-\$BUILD_NUMBER segunda-entrega-\$BRANCH_NAME_CLEANED:1.0.0-\$VERSION'
            }
        }
    }
}